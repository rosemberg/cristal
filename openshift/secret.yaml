# TEMPLATE — não commitar com valores reais de produção
# Preencher antes de aplicar: oc apply -f openshift/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: cristal-secrets
type: Opaque
stringData:
  CRISTAL_DATABASE_URL: "postgresql+asyncpg://cristal:SENHA@db-host:5432/cristal"
  CRISTAL_ADMIN_API_KEY: "CHAVE_ADMIN_PRODUCAO"
